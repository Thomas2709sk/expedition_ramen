<form method="post" action="{{ path('app_side_delete', {'id': side.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ side.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
